import { EscapeDirective } from './escape.directive';

describe('EscapeDirective', () => {
  it('should create an instance', () => {
    const directive = new EscapeDirective();
    expect(directive).toBeTruthy();
  });
});
