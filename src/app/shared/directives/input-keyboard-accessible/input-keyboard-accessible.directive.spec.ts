import { InputKeyboardAccessibleDirective } from './input-keyboard-accessible.directive';

describe('InputKeyboardAccessibleDirective', () => {
  it('should create an instance', () => {
    const directive = new InputKeyboardAccessibleDirective();
    expect(directive).toBeTruthy();
  });
});
