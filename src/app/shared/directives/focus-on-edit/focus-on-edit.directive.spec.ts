import { FocusOnEditDirective } from './focus-on-edit.directive';

describe('FocusOnEditDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusOnEditDirective();
    expect(directive).toBeTruthy();
  });
});
